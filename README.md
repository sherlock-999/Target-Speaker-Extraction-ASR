# Target-Speaker-Extraction-ASR

This project combines **SoloSpeech** (for target speaker extraction) with **Whisper Turbo** (for automatic speech recognition) to build an end-to-end speech pipeline.

---

### Clone the Repository
```bash
git clone https://github.com/sherlock-999/Target-Speaker-Extraction-ASR.git
cd Target-Speaker-Extraction-ASR
conda env create -f environment.yml
conda activate pipeline
